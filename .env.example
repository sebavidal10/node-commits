# GitHub Credentials
# Generate a Personal Access Token (classic) with 'repo' scope: https://github.com/settings/tokens
GITHUB_TOKEN=your_token_here
GITHUB_USERNAME=your_username

# Git Commit Identity (Optional - overrides global config)
# GIT_COMMITTER_NAME=Your Name
# GIT_COMMITTER_EMAIL=your@email.com

# Repository URL (Optional - usually detected automatically, but good for explicit override)
# GIT_REPO_URL=https://github.com/owner/repo.git

# Automation Logic
# Interval in milliseconds for regular script (npm start)
# GIT_REGULAR_INTERVAL=7200000

# Interval in milliseconds for PRs script (npm run prs)
# GIT_INTERVAL=5000

# Number of commits before creating a new branch in PRs script
# GIT_PRS_BATCH=10

# Base branch to fork from (default: main)
# GIT_BASE_BRANCH=main
